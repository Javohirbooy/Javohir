/**
 * Inline rasm (data URL) va avatar yuklash uchun yagona chegaralar.
 * Base64 taxminan fayl hajmining 4/3 qismi — `MAX_DATA_IMAGE_URL_CHARS` shunga mos.
 */
export const MAX_QUESTION_INLINE_IMAGE_FILE_BYTES = 12 * 1024 * 1024;

/** `QuestionRichText` ichida `data:image/...` URL uzunligi (belgilar). */
export const MAX_DATA_IMAGE_URL_CHARS = 18 * 1024 * 1024;

export const MAX_AVATAR_FILE_BYTES = 10 * 1024 * 1024;
